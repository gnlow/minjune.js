version: 2
jobs:
  build:
    docker:
      - image: circleci/node:12.7
      steps: 
        - checkout
        - restore_cache:
            keys:
              - dependency-cache-{{ checksum "package.json" }}
        - run: 
          name: test
          command: npm test